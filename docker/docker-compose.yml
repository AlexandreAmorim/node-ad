name: apilocal

services:
  pg:
    image: kartoza/postgis
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=apiuser
      - POSTGRES_PASSWORD=Segov2021
      - POSTGRES_DB=apilocal


