name: apilocal

services:
  pg:
    image: kartoza/postgis
    ports:
      - '5431:5432'
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=apilocal


